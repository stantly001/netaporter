import { ProductViewDirective } from './product-view.directive';

describe('ProductViewDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductViewDirective();
    expect(directive).toBeTruthy();
  });
});
